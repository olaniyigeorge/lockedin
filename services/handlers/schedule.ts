// Keep using feature-based folders: api/goals, api/lifedomains, etc — perfect.
// Encapsulate logic in service-layer files, not directly in routes. Move business logic from route.ts files into services/handlers/*.ts.
// Keep models and services loosely coupled — design each model/*.ts file as reusable blocks.
// Example: Move habit-task DB ops to services/habitTaskService.ts, import in api/habittasks/route.ts.





//create Prettier config  file, already started with eslint.config.mjs.

//Add unit tests in __tests__/ directory for utils/, models/, and services/.




