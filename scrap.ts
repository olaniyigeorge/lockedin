






    // markHabitTaskComplete(taskId: string): void {
    //     const task = this.habitTasks.find((t) => t.id === taskId);
    //     if (task) task.completed = true;
    // }

        // updateGoalProgress(domain: LifeDomain, goalId: string, progress: number): void {
    //     const goal = this.lifeDomains[domain].find((g) => g.id === goalId);
    //     if (goal) goal.progress = progress;
    // }

    // Daily Task Management
    // addDailyTask(task: DailyTask): void {
    //     this.dailyTasks.push(task);
    // }

    // markDailyTaskComplete(taskId: string): void {
    //     const task = this.dailyTasks.find((t) => t.id === taskId);
    //     if (task) task.completed = true;
    // }
